package Leas_Liudmila.abstraction;

// Create an abstract class called "Shape" with an abstract method named "calculateArea".
// Create two subclasses of Shape, such as "Rectangle" and "Circle", and implement the "calculateArea" method differently in each subclass.
// Create in Triangle method createRandomTriangle() which will generate a random Triangle object.
public abstract class Shape1 {
    public abstract double calculateArea();
}
